window.onload = function() {
    /* CookieManager.set('erwin', 'macaraig', 3);

    CookieManager.set('chkizna', 'gelica', 5);

    console.log(CookieManager.get('chkizna')); */

    /* CookieManager.set('erwin', 'pinaka pogi', 10);
    CookieManager.remove('erwin');
     */
    CookieManager.clear();
};
